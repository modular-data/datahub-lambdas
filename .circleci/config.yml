version: 2.1

orbs:
  reporting: ministryofjustice/hmpps-reporting@1.0.53
  slack: circleci/slack@4.12.5

workflows:
  checkout-build-publish:
    jobs:
    - reporting/gradle_owasp_check:
        tag: "11.0"
        notify_slack: false
        context:
        - dataworks-common
        cache_key: "dwh-lambdas-build-cache-v11-hmpps-test"
    - reporting/gradle_build_publish:
        tag: "11.0"
        app: datahub-lambdas
        app_artifacts_directory: build/libs/
        bucket_prefix: dataworks-artifact-store
        notify_slack: false
        context:
        - dataworks-common
        cache_key: "dwh-lambdas-build-cache-v11-hmpps-test"

  owasp-security:
    triggers:
    - schedule:
        cron: "30 6 * * *" ## Runs everyday at 7.30 AM UK TIME
        filters:
          branches:
            only:
            - main
    jobs:
    - reporting/gradle_owasp_check:
        tag: "11.0"
        notify_slack: false
        channel: dataworks-alerts
        context:
        - dataworks-common
        cache_key: "dwh-lambdas-owasp-build-cache-v8-hmpps-test"
